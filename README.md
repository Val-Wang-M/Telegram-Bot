# Telegram Fun Bot 🤖

A Python Telegram bot that responds to commands with fun interactions, games, and emojis.

## Features
- `/start` — greets the user
- `/cheat` — playful cheat warning
- `/rickroll` — sends a Rickroll link
- `/mood` — responds with a random mood
- `/flip` — flips a virtual coin
- `/insult` — playful random insults
- `/death` — generates humorous “death” scenarios
- `/emoji` — converts words into emojis using a built-in emoji dictionary

## Why I built it
To practice Python asynchronous programming and create a fun, interactive Telegram bot.

## How it was built
- Python 3.10+  
- `python-telegram-bot` library (async version)  
- Randomized responses and emoji conversion for interactive gameplay

## How to use
1. Set your bot token in a `.env` file (do NOT share it publicly)  
2. Run `bot.py`  
3. Chat with your bot on Telegram using the commands above!
